import { Typography } from "@mui/material";

/**
 * Espaco reservado para o modulo de Pedidos. O agregado Order e os casos de uso
 * entram nas fases seguintes.
 */
export default function OrderPage() {
  return <Typography variant="h6">Pedidos</Typography>;
}
